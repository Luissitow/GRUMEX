function inciarApp(e) {
    navegacionFija(),
    menuActivo(),
    carruselheader(),
    carrusel(),
    mensaje()
}
function menuActivo(e) {
    const t = document.querySelector(".navegacion__buttom")
      , c = document.querySelector(".navegacion__nav")
      , i = document.querySelector(".navegacion_estatica__buttom")
      , n = document.querySelector(".navegacion_estatica__nav")
      , o = (window.location.pathname.startsWith("/manufactura"),
    window.location.pathname.startsWith("/manufactura/servicio"))
      , a = window.location.pathname.startsWith("/almacenamiento/servicio");
    o || a ? i.addEventListener("click", (function() {
        n.classList.toggle("active")
    }
    )) : t.addEventListener("click", (function() {
        c.classList.toggle("active")
    }
    ))
}

function navegacionFija() {
    const navegacion = document.querySelector(".navegacion"); // Menú de navegación
    const whatsapp = document.querySelector(".whatsapp"); // Botón de WhatsApp

    // Esperar a que termine el preloader antes de fijar la navegación
    setTimeout(() => {
        navegacion.classList.add("fijo", "mostrar"); // Fijar navegación después de la animación del preloader
    }, 1900); // 2000 ms (2 segundos) = tiempo que dura el preloader

    // Mostrar el botón de WhatsApp al hacer scroll hacia abajo
    window.addEventListener("scroll", () => {
        if (window.scrollY > 100) { // Ajusta "100" según el punto donde quieres que aparezca
            whatsapp.classList.add("fijo"); // Agregar clase para hacerlo visible
        } else {
            whatsapp.classList.remove("fijo"); // Quitar clase para ocultarlo
        }
    });
}

function carruselheader() {
    if (document.querySelector("#carruselheader")) {
        let t = document.querySelectorAll(".hslider .carrusel-header__list  .hitem ")
            , c = document.getElementById("hnext")
            , i = document.getElementById("hprev")
            , n = t.length
            , o = 0;
        c.onclick = function() {
            o += 1,
            o >= n && (o = 0),
            e()
        }
        ,
        i.onclick = function() {
            o -= 1,
            o < 0 && (o = n - 1),
            e()
        }
        ;
        let a = setInterval(()=>{
            c.click()
        }
        , 3e3);
        function e() {
            document.querySelector(".hslider .carrusel-header__list .hitem.hactive ").classList.remove("hactive"),
            t[o].classList.add("hactive")            
            clearInterval(a),
            a = setInterval(()=>{
                c.click()
            }
            , 3e3)
        }
    }
}
function carrusel() {
    if (document.querySelector("#carrusel")) {
        let t = document.querySelectorAll(".cslider .carrusel__list  .citem ")
          , c = document.getElementById("cnext")
          , i = document.getElementById("cprev")
          , n = document.querySelectorAll(".cthumbnail .citem")
          , o = t.length
          , a = 0;
        c.onclick = function() {
            a += 1,
            a >= o && (a = 0),
            e()
        }
        ,
        i.onclick = function() {
            a -= 1,
            a < 0 && (a = o - 1),
            e()
        }
        ;
        let s = setInterval(()=>{
            c.click()
        }
        , 4e3);
        function e() {
            let e = document.querySelector(".cslider .carrusel__list .citem.cactive ")
              , i = document.querySelector(".cthumbnail .citem.cactive");
            e.classList.remove("cactive"),
            i.classList.remove("cactive"),
            n[a].classList.add("cactive"),
            t[a].classList.add("cactive"),
            clearInterval(s),
            s = setInterval(()=>{
                c.click()
            }
            , 4e3)
        }
        n.forEach((t,c)=>{
            t.addEventListener("click", ()=>{
                a = c,
                e()
            }
            )
        }
        )
    }
}
function mensaje(e) {
    var t = document.getElementById("parrafo");
    setTimeout((function() {
        t.style.opacity = 0,
        setTimeout((function() {
            t.remove()
        }
        ), 1e3)
    }
    ), 5e3)
}
document.addEventListener("DOMContentLoaded", (function(e) {
    inciarApp()
}
)),
function() {
    if (document.querySelector("#exitos")) {
        let t = document.querySelectorAll(".slider .exitos__list  .item ")
          , c = document.querySelectorAll(".sliderdos .exitos__lists  .item ")
          , i = document.getElementById("next")
          , n = document.getElementById("prev")
          , o = document.querySelectorAll(".thumbnail .item");
        const a = document.querySelector(".thumbnail");
        let s, l, r = !1;
        const d = e=>{
            r = !0,
            a.classList.add("dragging"),
            s = e.pageX,
            l = a.scrollLeft
        }
          , u = e=>{
            r && (a.scrollLeft = l - (e.pageX - s))
        }
          , m = ()=>{
            r = !1,
            a.classList.remove("dragging")
        }
        ;
        let v = t.length
          , f = 0;
        i.onclick = function() {
            f += 1,
            f >= v && (f = 0),
            e()
        }
        ,
        n.onclick = function() {
            f -= 1,
            f < 0 && (f = v - 1),
            e()
        }
        ;
        let h = setInterval(()=>{
            i.click()
        }
        , 8e3);
        function e() {
            let e = document.querySelector(".slider .exitos__list .item.active ")
              , n = document.querySelector(".thumbnail .item.active")
              , a = document.querySelector(".sliderdos .exitos__lists  .item.active");
            e.classList.remove("active"),
            n.classList.remove("active"),
            a.classList.remove("active"),
            t[f].classList.add("active"),
            o[f].classList.add("active"),
            c[f].classList.add("active"),
            clearInterval(h),
            h = setInterval(()=>{
                i.click()
            }
            , 8e3)
        }
        o.forEach((t,c)=>{
            t.addEventListener("click", ()=>{
                f = c,
                e()
            }
            )
        }
        ),
        a.addEventListener("mousedown", d),
        a.addEventListener("mousemove", u),
        document.addEventListener("mouseup", m)
    }
}();
//# sourceMappingURL=bundle.js.map
